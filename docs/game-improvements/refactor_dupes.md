1. **Helper de conversión de formas de colisión** (`packages/core/src/physics/collision/BroadPhase.ts`, `packages/core/src/physics/collision/CollisionSystems.ts`): Extraer una función compartida `toShapeDefinition(collider2D: Collider2DComponent): ColliderComponent["shape"]` (ubicarla en un módulo común de física, p.ej. `packages/core/src/physics/utils/`) que unifique la conversión `circle`→`ShapeType.Circle`, `aabb`→`ShapeType.Box` (halfWidth*2/halfHeight*2). Reemplazar `BroadPhase.resolveColliderLike` (líneas ~133-164) y `CollisionSystems.resolveCollider` (líneas ~279-327) para usar el helper.

2. **Lifecycle unload→load de escena** (`packages/core/src/scenes/SceneManager.ts`): Los métodos `transitionTo`, `push`, `pop`, `replace` (líneas ~435-585) repiten el patrón `state=UNLOADING → onExit → state=LOADING → onWorldCreated → onEnter → updateStack`. Extraer un método privado `performSceneSwap(params: { oldScene?: Scene; newScene?: Scene; token: number; skipExit?: boolean; skipEnter?: boolean })` que centralice esta secuencia, y refactorizar los 4 métodos públicos para delegar en él, preservando el comportamiento exacto (incluyendo los checks de `token !== this.transitionToken` y las emisiones de progreso 0.3/0.7).

3. **Dither/grid transitions** (`packages/core/src/scenes/transitions/DitherTransition.ts`, `packages/core/src/scenes/transitions/RetroGridTransition.ts`): Extraer una función compartida `iterateGridBlocks(width, height, blockSize, callback: (col, row, cellX, cellY) => void)` en `packages/core/src/scenes/transitions/GridTransitionUtils.ts` (nuevo archivo) que reemplace el doble bucle `for (r) for (c)` de cálculo de columnas/filas en ambas clases.

4. **Restore de componentes AoS/SoA** (`packages/core/src/snapshots/SnapshotRestore.ts`, `packages/core/src/snapshots/SnapshotRestoreSoA.ts`): Crear en `packages/core/src/snapshots/SnapshotInternalAccess.ts` (o archivo similar ya existente) una función `restoreComponentStorage(internal: InternalWorldAccess, type: string): { storage: Map; index: Set; versions: Map }` que inicialice y registre `componentMaps`/`componentIndex`/`componentVersions` para un tipo de componente, y otra función/inline helper `registerEntityComponent(internal, entityId, type)` que rellene `entityComponentSets`. Usar ambos helpers en `SnapshotRestore.restore` (líneas ~44-71) y `SnapshotRestoreSoA.restoreSoAComponent` (líneas ~28-55).

5. **Unificación de culling espacial** (`packages/core/src/systems/SpatialCullingSystem.ts`): Extraer método estático `getExpandedViewport(world, margin): { minX, minY, maxX, maxY }` que combine `getViewport()` con la expansión por margen (actualmente repetido en `isEntityInViewport` líneas ~108-121 y `filterInViewport` líneas ~138-147). Extraer también `isEntityInViewportBounds(world, entity, bounds): boolean` que encapsule el test de `Transform` contra los bounds ya calculados (usado en `isEntityInViewport`, `filterInViewport`, y el bucle del método `update` líneas ~241-255). Refactorizar los 4 sitios para usar estos helpers, incluyendo el bloque duplicado de `ScreenConfig`/`screenWidth`/`screenHeight` en `getViewport` (líneas ~47-50) y `update` (líneas ~213-216).

6. **EntityPool de proyectiles unificado** (`src/games/asteroids/EntityPool.ts`, `src/games/geometrywars/EntityPool.ts`, `src/games/space-invaders/EntityPool.ts`): Ya existe `ProjectilePool<TParams>` en `@tiny-aster/core` usado por geometrywars y space-invaders. Crear una función factory `createProjectilePoolConfig(defaults: { shape: string; layer: number; mask: number; poolId: string; damageCategory: string; faction: "player"|"enemy"; isTrigger?: boolean; extraComponents?: (data) => void })` en un módulo compartido de `packages/gameplay-kit` (p.ej. `packages/gameplay-kit/src/pooling/ProjectilePoolConfig.ts`) que genere el objeto `{factory, reset, initializer}` con Transform/Velocity/Render/Collider/TTL/Reclaimable/CollisionEvents/Damage/Faction ya poblados. Refactorizar `asteroids/EntityPool.ts` (líneas 32-139), `geometrywars/EntityPool.ts` (líneas 27-147) y `space-invaders/EntityPool.ts` (`createBulletPoolConfig`, líneas 19-82) para usar este factory compartido, pasando solo las diferencias específicas de cada juego como parámetros u overrides post-creación.

7. **Helpers de dibujo Canvas/Skia comunes (ball/paddle)** (`src/games/pong/rendering/PongCanvasVisuals.ts`, `src/games/pong/rendering/PongSkiaVisuals.ts`, `src/games/shared/rendering/CanvasNeonUtils.ts`): Ya existe `drawNeonShape` en `CanvasNeonUtils.ts` usado parcialmente por `drawPongPaddle`. Extender ese patrón: crear equivalente Skia `drawNeonShapeSkia` en un archivo hermano (p.ej. `SkiaNeonUtils.ts`) para eliminar la duplicación marcada con TODOs entre `drawPongPaddle`/`drawSkiaPongPaddle` y `drawPongBall`/`drawSkiaPongBall` (ver comentarios `Ref: cceb5e9c`, `Ref: 18e6e604`, `Ref: 6ffd39a8` en `PongCanvasVisuals.ts` y `PongSkiaVisuals.ts`). Extender también a `geometrywars/rendering/GeometryWarsCanvasVisuals.ts`/`GeometryWarsSkiaVisuals.ts` que comparten patrones similares de "ball" (ver Ref: `0f5b5e6b`, `8a13e9cc`).

8. **Lógica de daño/invulnerabilidad de platformer** (`src/games/echorunner/EchoRunnerGame.ts` clase `EchoRunnerDamageSystem` líneas ~123-191, `src/games/platformer/systems/PlatformerDamageSystem.ts` líneas ~7-31): Extraer una función compartida `updatePlayerInvulnerabilityAndContactDamage(world, deltaTime, config?: { contactDistance?: number; invulnerabilityDuration?: number; damageAmount?: number })` en un módulo común de gameplay-kit (p.ej. `packages/gameplay-kit/src/combat/systems/ContactDamageUtils.ts`) que contenga: el tick de `invulnerableRemaining` vía `PhysicsUtils.tickTimer`, el bucle de detección de contacto por distancia con enemigos, y la aplicación de daño + hitFlash + screenshake. Ambos sistemas deben delegar en esta función, exponiendo solo callbacks/config para las diferencias específicas (sonido, screenshake intensity/duration).

9. **Helpers de efectos VFX compartidos** (`src/games/shared/rendering/SharedVFX.ts`): El archivo ya tiene 15+ TODOs de duplicación interna documentados (ver comentarios en líneas ~540-620, ~888-924, ~1233-1289, y otros marcados `Ref: d45cd223`, `Ref: 6a369da9`, `Ref: ff4fc95f`, etc. — usar `grep "TODO(refactor)" src/games/shared/rendering/SharedVFX.ts` para localizar todos). Para cada par Canvas/Skia de un mismo efecto (warp lines, matrix rain, vortex/singularity, debris shockwave, etc.), extraer la lógica de actualización de estado (movimiento, wraparound, reinicialización aleatoria) a una función pura compartida entre ambos backends, dejando solo las llamadas de dibujo (`ctx.*` vs `canvas.*`/`paint.*`) específicas de cada uno.

10. **BaseMiniGameEncounter compartido** (`src/games/geometrywars/story/GeometryWarsEncounter.ts`, `src/games/space-invaders/story/InvasionEncounter.ts`): Ambas clases duplican casi exactamente los métodos `onResult(callback)`, `emitResult(context, payload)` y `dispose()` (ver `report/jscpd-report.json` entradas líneas 5072-5144, con fragmentos idénticos salvo el nombre de la propiedad `geomsCollected` vs equivalente). Crear una clase base abstracta `BaseMiniGameEncounter` (ubicarla en un módulo compartido de story/encounter, p.ej. `packages/gameplay-kit/src/story/BaseMiniGameEncounter.ts` o similar ubicación ya usada por el sistema de story) con: propiedad protegida `resultCallback`, método `onResult(callback)`, método `emitResult(context, payload)` parametrizable con un método abstracto/hook `protected buildResultPayload(payload): Partial<MiniGameResult>` para las diferencias específicas de cada juego (ej. `geomsCollected`), y método `dispose()` con la lógica de destroy/stop del juego interno. Refactorizar `GeometryWarsEncounter` e `InvasionEncounter` para extender esta clase base.
